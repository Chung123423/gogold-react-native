import { StyleSheet } from 'react-native';

const global_style = StyleSheet.create({
    app_container: {
        backgroundColor: 'red',
        display: 'flex',
        height: '100%',
        paddingLeft: 20,
        paddingRight: 20,
    }
})

export default global_style;